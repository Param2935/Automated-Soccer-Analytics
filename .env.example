# Run mode: "live" (default) hits the API, "test" uses the mock generator.
APP_MODE=live

# Football-Data.org API key — get a free one at https://www.football-data.org/
# Required for live mode only; test mode needs no key.
FOOTBALL_API_KEY=your_key_here
